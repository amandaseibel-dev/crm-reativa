# 20260917160417 — reconstrução da parcela paga antes da extração (PR #403)

| | |
|---|---|
| **Versão** | `20260917160417` |
| **Nome em produção** | `parcela_paga_antes_da_extracao` |
| **SQL exato** | [`20260917160417__parcela_paga_antes_da_extracao.sql`](20260917160417__parcela_paga_antes_da_extracao.sql) |
| **md5 (statement de produção)** | `696880ed0df988141ebc2797ff1a6261` |
| **SHA256 (statement de produção)** | `38f1b378252e15227e5a9f4bc736b3d2aab5c4c22b4ac68c95cc1a9081cebcb0` |
| **Bytes** | **59.025 bytes** em UTF-8 |
| **Caracteres** | **59.004** (a diferença vem de `·`, `—` e acentos) |
| **Statements** | 1 |
| **Aplicada em** | 2026-09-17 16:04:17 UTC |
| **Classificação** | `ESTRUTURAL` |
| **Par na trilha executável** | [`supabase/migrations/20260917200000_parcela_paga_antes_da_extracao.sql`](../../migrations/20260917200000_parcela_paga_antes_da_extracao.sql): mesmo SQL, version diferente |

A version difere da do arquivo em `supabase/migrations/` porque o
`apply_migration` grava a version pelo relógio UTC do instante. O statement de
produção não tem o newline final; o arquivo deste diretório tem, pela convenção
POSIX do `LEIA-ME.md`.

## CONTEXTO

Implantação do PR #403, autorizada pela gestão em 17/09/2026. A etapa
`reconstruir_parcela_paga_antes` entrou **desligada**. Ela foi ligada depois,
na versão `20260917161116`.

O relatório de títulos em aberto não traz a parcela que já estava paga na
extração. Nos acordos 72113 e 72153, a entrada paga caiu em REVISAO ("não tem
parcela livre").

**Preflight** (leitura, 16:00 UTC):
- CI remoto verde no head `dd45519`; `main` parado em `e206c34`;
- md5 dos 13 corpos de que a migration depende iguais aos da base:
  - motor `fa3d64ad`;
  - prévia do à vista `9e062e76`;
  - registro do à vista `beb45df8`;
  - `pagamentos_trava` `33e19e85`;
  - `_pagamentos_baixar_lote` `6a0a351c`;
  - `fluxo_pagamentos_rodar` `8255c8d4`;
  - `completar_parcelas_acordo` `1e4c6853`;
  - `importar_acordos` `1bb69aed`;
  - `baixa_pelo_relatorio_pagamento` `de838dc8`;
  - `conciliacao_reprocessar` `e1475551`;
  - `_pagamento_conciliar` `fb72abd1`;
  - `vencimento_do_pagamento` `4f4668a6`;
  - `documento_casa_com_parcela` `8dc19271`;
- nenhuma das 4 funções novas existia, nem a linha da etapa;
- 120 gatilhos (md5 `1c734c44`), 29 jobs no cron (md5 `0ea493b2`), reposição
  desligada, outras etapas `674351f9`;
- 57 pagamentos pendentes; 72113 e 72153 em REVISAO.

## O QUE FEZ

- **Criou:**
  - `parcela_paga_antes_previa` (SQL STABLE, 14 evidências, `diagnostico`);
  - `parcela_paga_antes_reconstruir`;
  - `parcela_paga_antes_reconstruir_pendentes(integer, uuid[])`;
  - `parcela_paga_antes_apos_importar_acordos`.

  Nenhuma é chamável por anon ou authenticated.
- **Trocou o corpo, só com acréscimo:**
  - `_pagamentos_baixar_lote` e `fluxo_pagamentos_rodar`: etapa depois da reconciliação;
  - `completar_parcelas_acordo`: boleto confiável quando o prefixo é o número do acordo, só no INSERT;
  - `importar_acordos`: bloco final protegido.
- **Inseriu** a etapa `reconstruir_parcela_paga_antes` com `ligado = false`.
- Nenhum dado de pagamento, parcela, acordo ou fila foi escrito.

## RESULTADO MEDIDO

**Depois** (leitura, 16:04 UTC):
- statement com md5 `696880ed`, igual ao arquivo executável sem o newline final;
- corpos:
  - `parcela_paga_antes_previa` `216dda2d`;
  - `parcela_paga_antes_reconstruir` `0bd85569`;
  - `parcela_paga_antes_reconstruir_pendentes` `b2c955b4`;
  - `parcela_paga_antes_apos_importar_acordos` `a1b34da6`;
  - `_pagamentos_baixar_lote` `6df42e17`;
  - `fluxo_pagamentos_rodar` `418c1529`;
  - `completar_parcelas_acordo` `5ccd7de7`;
  - `importar_acordos` `d858a3b1`;
- ACL das trocadas idêntica; `importar_acordos` segue com `authenticated`;
- motor, prévia e registro do à vista e `pagamentos_trava` iguais ao preflight;
- gatilhos, cron e demais etapas iguais ao preflight; reposição desligada;
- hashes de acordos, parcelas, pagamentos, fila e 62866 iguais ao preflight;
  0 auditorias `RECONSTRUCAO%`.

Merge do PR (`02e3ad4`) às 16:04:55 UTC.

**Auditoria com a etapa desligada** (leitura; prévia implantada provada por md5,
consulta `afe71e10`):
- 57 pagamentos pendentes analisados;
- 2 aprovados: 72113 (pagamento `799cdba3`) e 72153 (`819ebd91`);
- 55 recusados:
  - `PARCELA_PAGA_ANTES_ACORDO_AUSENTE` 43 (AGUARDANDO_ACORDO);
  - `PARCELA_PAGA_ANTES_BOLETO_JA_TEM_PARCELA` 9 (PARCELA_JA_PAGA);
  - `PARCELA_PAGA_ANTES_ACORDO_NAO_IMPORTADO` 3 (PARCELA_JA_PAGA);
- nenhum recusado era candidato da etapa (acordo ativo e boleto sem parcela).

## COMO RECONFERIR

```sql
select version, name, array_length(statements, 1) as n,
       md5(array_to_string(statements, E'\n')) as md5_prod
  from supabase_migrations.schema_migrations
 where version = '20260917160417';
```

```bash
perl -0pe 's/\n\z//' supabase/ledger/2026-09/20260917160417__parcela_paga_antes_da_extracao.sql | md5 -q
```

Os dois devolvem `696880ed0df988141ebc2797ff1a6261`.
