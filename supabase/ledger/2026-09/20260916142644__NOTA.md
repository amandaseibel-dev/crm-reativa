# 20260916142644 — retomada de `baixa_pelo_relatorio` após o reprocessamento dos 69

| | |
|---|---|
| **Versão** | `20260916142644` |
| **Nome em produção** | `retoma_baixa_pelo_relatorio_apos_reprocessamento_69` |
| **SQL exato** | [`20260916142644__retoma_baixa_pelo_relatorio_apos_reprocessamento_69.sql`](20260916142644__retoma_baixa_pelo_relatorio_apos_reprocessamento_69.sql) |
| **md5 (statement de produção)** | `7da9b4fe5b85263348802b013eb888e1` |
| **SHA256 (statement de produção)** | `eaa5f04af7579773f3062de6bd2a212e059eec22f63447593b5c4f3ccda3da58` |
| **Bytes** | **7.348 bytes** em UTF-8 |
| **Caracteres** | **7.345** (a diferença vem dos acentos do texto da pausa) |
| **Statements** | 1 |
| **Aplicada em** | 2026-09-16, 14:26:44 UTC |
| **Classificação** | `CORRECAO_DADO` |
| **Par na trilha executável** | **nenhum, de propósito** — é alteração de dado de configuração, como a pausa `20260916112756` |

O statement de produção **já termina em newline** (o bloco foi enviado com a
quebra de linha final do arquivo preparado). O arquivo deste diretório tem um
`\n` a mais, pela convenção POSIX do `LEIA-ME.md`; a reconferência com `perl`
devolve o md5 de produção.

## O QUE FOI

Encerramento da pausa temporária `20260916112756`. Uma única linha de
`fluxo_pagamentos_config` mudou: a etapa `baixa_pelo_relatorio` voltou a
`ligado = true`, com a observação restaurada **byte a byte** para a que existia
antes da pausa:

`le o numero do documento no pagamento, grava na parcela e baixa -- so quando o valor bate e o acordo esta ATIVO`

`alterado_em = now()` e `alterado_por = 'retomada_20260916_gestao'`, por decisão
da gestão. A observação da pausa ficou registrada em `auditoria`
(`RELIGOU_ETAPA_FLUXO`), junto da observação restaurada.

Estado da etapa antes da pausa, confirmado em três fontes (leitura da
configuração às 09:58 de 16/09, `auditoria` `DESLIGOU_ETAPA_FLUXO` das 11:27:56 e
o `ESTADO ANTERIOR` gravado pela pausa): `ligado = true`, a observação acima,
`alterado_em = 2026-09-01 13:03:56.83637+00`, `alterado_por = null`.

## O QUE RODOU

Um bloco `DO`, nesta ordem:

1. `lock_timeout` de 3 s;
2. exige a etapa exatamente como a pausa a deixou: `ligado = false`,
   `alterado_por = 'protecao_20260916_gestao'` e observação byte a byte igual;
3. exige 0 `AGUARDANDO_AMARRACAO`, a fila do motor exatamente
   `AGUARDANDO_ACORDO=38;PARCELA_JA_PAGA=6` e o SHA256 dos 44 pagamentos auditados
   (`77e1ee57…6077e`, sobre id, status e valor);
4. exige o motor (`fa3d64ad…`) e os gatilhos `trg_recalc_parcela` e
   `trg_acordo_fecha_com_a_ultima_parcela` em `O` com os md5 aprovados;
5. aborta se houver rodada de `fluxo_pagamentos_horario` em andamento;
6. `update` só dessa linha, exigindo `row_count = 1`; `insert` em `auditoria`;
7. **prova**: as outras 6 etapas idênticas, estado final exato de todas as etapas
   e observação restaurada.

## RESULTADO MEDIDO

Logo depois da aplicação (14:27 UTC, leitura):

| Conferência | Resultado |
|---|---|
| `baixa_pelo_relatorio` | `true` · `retomada_20260916_gestao` · observação restaurada exatamente |
| `RELIGOU_ETAPA_FLUXO` em `auditoria` | exatamente 1 |
| outras 6 etapas | byte a byte iguais (hash `3eb88883…` antes e depois) |
| fila do motor | `AGUARDANDO_ACORDO=38;PARCELA_JA_PAGA=6`, SHA256 dos 44 inalterado |
| `AGUARDANDO_AMARRACAO` | 0 |
| baixas causadas pela alteração | nenhuma (90 pagamentos `BAIXADO`, 4.520 parcelas `PAGO`, iguais a antes) |

Primeira rodada horária natural depois da retomada, 14:40 UTC (nenhuma execução
manual):

| Conferência | Resultado |
|---|---|
| execução | `succeeded`, origem `cron`, sem erro |
| `baixa_pelo_relatorio` | modo aplicado, **44 avaliados, 0 baixados** |
| fila | 38 `AGUARDANDO_ACORDO`, 6 `PARCELA_JA_PAGA`; SHA256 dos 44 inalterado |
| `AGUARDANDO_AMARRACAO` | 0 |
| `consulta_portador` | pulou (`PAUSADO_PARA_MAPEAMENTO_DE_ACORDOS`) |
| saldo total da carteira | igual antes e depois da rodada |

## LIMITE DA PAUSA, MEDIDO DEPOIS

A pausa `20260916112756` desligou a etapa lida por `fluxo_pagamentos_rodar`, mas
**não** alcança o caminho da importação de pagamentos: o gatilho de comando
`trg_pagamentos_baixar_lote` (AFTER INSERT em `pagamentos`) chama
`_pagamentos_baixar_lote`, que executa
`baixa_pelo_relatorio_pagamento(true, current_date - 180)` sem ler
`fluxo_pagamentos_config` — e o gatilho de linha `trg_pagamento_conciliar` chama
`pagamento_conciliar_um(new.id, true)` para cada pagamento inserido.

Durante a pausa (11:27:56 a 14:26:44 UTC) **nenhum pagamento foi inserido**,
então a pausa valeu na prática. Uma pausa futura que precise segurar o motor
também durante importações terá de tratar esse caminho.

## COMO RECONFERIR

```sql
select version, name, array_length(statements, 1) as n,
       md5(statements[1]) as md5_prod,
       octet_length(statements[1]) as bytes, length(statements[1]) as chars
  from supabase_migrations.schema_migrations
 where version = '20260916142644';
```

```bash
perl -0pe 's/\n\z//' supabase/ledger/2026-09/20260916142644__*.sql | md5 -q
```

Os dois devolvem `7da9b4fe5b85263348802b013eb888e1`.
