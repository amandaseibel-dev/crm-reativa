# 20260917161116 — ativação de `reconstruir_parcela_paga_antes`

| | |
|---|---|
| **Versão** | `20260917161116` |
| **Nome em produção** | `ativa_reconstrucao_parcela_paga_antes` |
| **SQL exato** | [`20260917161116__ativa_reconstrucao_parcela_paga_antes.sql`](20260917161116__ativa_reconstrucao_parcela_paga_antes.sql) |
| **md5 (statement de produção)** | `eb4638414cc2e22ecc3751befc4751de` |
| **SHA256 (statement de produção)** | `6fb909b899af9431407416c3e21243410de39e965b01612b5c8f2a877de1d3b8` |
| **Bytes** | **7.928 bytes** em UTF-8 (só ASCII) |
| **Statements** | 1 |
| **Aplicada em** | 2026-09-17 16:11:16 UTC |
| **Classificação** | `CORRECAO_DADO` |
| **Par na trilha executável** | **nenhum, de propósito**: é alteração de dado de configuração, como a retomada `20260916142644` |

O statement de produção não tem o newline final; o arquivo deste diretório tem,
pela convenção POSIX do `LEIA-ME.md`.

## CONTEXTO

A gestão autorizou ligar a etapa se os únicos aprovados na prévia fossem os
casos esperados. Na auditoria da versão `20260917160417`, feita com a etapa
desligada, os aprovados foram exatamente 72113 e 72153 (2 de 57). Os 55
recusados estavam fora do alcance da etapa.

## O QUE RODOU

Um bloco `DO`, nesta ordem:

1. `lock_timeout` de 3 s.
2. Exige o motor e as três funções da reconstrução com os md5 implantados.
3. Exige a etapa como a migration deixou: `ligado = false` e
   `alterado_por = 'migration_20260917200000'`.
4. Exige a fila do motor exatamente `AGUARDANDO_ACORDO=43;PARCELA_JA_PAGA=12;REVISAO=2`
   e o SHA256 dos 57 auditados (`21686198…881ac25`, sobre id, status e valor).
5. Aborta se houver rodada de `fluxo_pagamentos_horario` em andamento.
6. `update` só dessa linha (`ligado = true`,
   `alterado_por = 'ativacao_20260917_gestao'`, `row_count = 1`) e `insert` em
   `auditoria` (`LIGOU_ETAPA_FLUXO`). Prova de que as outras etapas ficaram
   byte a byte iguais.
7. Roda `parcela_paga_antes_reconstruir_pendentes(50)` na hora, sem esperar as
   :40. Exige 2 avaliados, 2 reconstruídas, 0 erros e exatamente os boletos
   `50721130001` e `50721530001`.
8. Prova do estado final dos dois acordos e da fila restante
   (`AGUARDANDO_ACORDO=43;PARCELA_JA_PAGA=12`).

Qualquer desvio desfaria tudo, inclusive a ativação.

## RESULTADO MEDIDO

Leitura às 16:11 UTC:

| | 72113 | 72153 |
|---|---|---|
| parcela 1 | boleto `50721130001`, R$ 2.000,00, venc. 18/09, confiável, PAGO em 16/09 | boleto `50721530001`, R$ 2.193,05, venc. 18/09, confiável, PAGO em 16/09 |
| vínculo | `origem_baixa` GATILHO_IMPORTACAO, referência = o próprio pagamento | idem |
| crédito / honorário | cobranca12 / 148,15 | cobranca11 / 162,45 |
| acordo | ATIVO, 4/3.356,23 → **5/5.356,23**, soma das parcelas = total | ATIVO, 6/3.289,58 → **7/5.482,63**, soma = total |
| em aberto / saldo do acordo | 3.356,23 / 3.356,23 (inalterados) | 3.289,58 / 3.289,58 (inalterados) |
| pagamento | BAIXADO, sem motivo | BAIXADO, sem motivo |
| fila | RESOLVIDO_AUTOMATICO por conciliacao@sistema | idem |
| aluno | AGUARDANDO_BAIXA → ACORDO_FECHADO (gatilho de reabertura), saldo 7.769,74 inalterado | idem, saldo 11.462,92 inalterado |

Totais:
- `RECONSTRUCAO_PARCELA_PAGA_ANTES_DA_EXTRACAO` 2; `LIGOU_ETAPA_FLUXO` 1;
- 0 motivos com prefixo na fila;
- 15.229 parcelas (+2) e 4.051 acordos (iguais);
- 62866 com hash inalterado;
- gatilhos, cron e demais etapas iguais;
- reposição desligada, com os mesmos 9 pedidos pendentes.

## COMO RECONFERIR

```sql
select version, name, array_length(statements, 1) as n,
       md5(array_to_string(statements, E'\n')) as md5_prod
  from supabase_migrations.schema_migrations
 where version = '20260917161116';
```

```bash
perl -0pe 's/\n\z//' supabase/ledger/2026-09/20260917161116__ativa_reconstrucao_parcela_paga_antes.sql | md5 -q
```

Os dois devolvem `eb4638414cc2e22ecc3751befc4751de`.
