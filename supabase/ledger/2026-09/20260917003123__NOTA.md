# 20260917003123 — Recuperação de acordo à vista pago sem importação (PR #399)

| | |
|---|---|
| **Versão** | `20260917003123` |
| **Nome em produção** | `recuperacao_acordo_pago_sem_importacao` |
| **SQL exato** | [`20260917003123__recuperacao_acordo_pago_sem_importacao.sql`](20260917003123__recuperacao_acordo_pago_sem_importacao.sql) |
| **md5 (statement de produção)** | `655f169b6a1e4178ecca1353ab9c367a` |
| **SHA256 (statement de produção)** | `d547767312ff9226c21fe2d6ae39694a8d03e1770930b36dfedabfead8eae90c` |
| **Bytes** | **43,346 bytes** em UTF-8 |
| **Caracteres** | **43,240** |
| **Statements** | 1 |
| **Aplicada em** | 2026-09-17 00:31:23 UTC |
| **Classificação** | `ESTRUTURAL` |
| **Par na trilha executável** | [`supabase/migrations/20260916230000_recuperacao_acordo_pago_sem_importacao.sql`](../../migrations/20260916230000_recuperacao_acordo_pago_sem_importacao.sql) — mesmo SQL, version diferente |

A version difere da do arquivo em `supabase/migrations/` porque o
`apply_migration` grava a version pelo relógio UTC do instante. A diferença é
preservada de propósito. O statement de produção não tem o newline final; o
arquivo deste diretório tem, pela convenção POSIX do `LEIA-ME.md`.

## CONTEXTO

Implantação técnica do PR #399 (merge `d09c0527`, 2026-09-17 00:28:57 UTC),
autorizada pela gestão em 16/09/2026 **sem registro de acordo real**: nesta
fase só as funções foram criadas.

**Preflight** (leitura, antes do merge):
- head do PR = `027fad4`; `main` sem avanço desde a base;
- CI remoto verde;
- md5 do `prosrc` das 13 dependências igual às fixtures dos testes de
  comportamento. São elas:
  - `pagamento_conciliar_um` `fa3d64ad`
  - `vincular_titulos_acordo` `47125b28`
  - `pagamento_vincular_aluno` `341df31b`
  - `documento_casa_com_parcela` `8dc19271`
  - `vencimento_do_pagamento` `4f4668a6`
  - `usuario_e_gestao` `889cfa4a`
  - `_titulo_quita_com_o_acordo` `bde4388c`
  - `_acordo_fecha_com_a_ultima_parcela` `9a230134`
  - `_bloquear_parcela_baixa_acordo_encerrado` `eea504e2`
  - `_parcela_pago_em_automatico` `25bf0fb4`
  - `_reabrir_aluno_com_divida_nova` `57ff5772`
  - `tg_acordo_bloquear_duplicado` `1cbfef05`
  - `_acordo_herda_responsavel_do_aluno` `054ac6e1`
- nenhuma das três funções novas existia;
- Postgres 17.6.

## O QUE FEZ

Criou três funções e não escreveu dado nenhum.

- `acordo_avista_previa(uuid, uuid[])`: SQL puro e STABLE. `prosrc` md5
  `9e062e76`. EXECUTE só para `postgres` e `service_role`.
- `acordo_avista_registrar(uuid, uuid[], boolean)`: plpgsql, `statement_timeout`
  60s. `prosrc` md5 `beb45df8`. EXECUTE para `authenticated`, com portão interno
  da gestão.
- `pagamentos_trava(uuid[])`: plpgsql e STABLE. `prosrc` md5 `482299bf`.
  EXECUTE para `authenticated`, com portão interno da gestão ou service_role.

A explicação completa da regra está no cabeçalho do próprio SQL.

**Depois** (leitura, 00:34 UTC):
- 1 sobrecarga de cada função nova;
- nenhuma executável por `anon` nem por PUBLIC;
- a prévia não é executável por `authenticated`.

Ficaram iguais ao preflight:
- o md5 das 13 dependências e das 12 funções que o PR não podia tocar:
  - `importar_acordos`
  - `conciliacao_consultar_portador_pendentes`
  - `baixa_pelo_relatorio_pagamento`
  - `conciliacao_reprocessar`
  - `fluxo_pagamentos_rodar`
  - `parcelas_amarrar_boleto`
  - `_pagamento_conciliar`
  - `_pagamentos_baixar_lote`
  - `trg_pagamentos_gerar_confirmacao`
  - `pagamentos_sem_aluno`
  - `completar_parcelas_acordo`
  - `acordos_pos_importacao`
- os 56 gatilhos das seis tabelas do fluxo (md5 `b2604267`);
- os 29 jobs do cron (md5 `1310fec7`);
- `fluxo_pagamentos_config` (md5 `674351f9`), com `consulta_portador` desligada;
- os dados:
  - 4.028 acordos, 15.180 parcelas e 4.658 vínculos;
  - 0 auditorias `RECUPERACAO_ACORDO_PAGO_SEM_IMPORTACAO`;
  - 0 acordos com os números 72066, 72078, 72080, 72097 e 71752;
  - hashes dos 5 pagamentos, dos 5 alunos, das mensalidades, da fila e das
    confirmações iguais aos de antes.

**Simulação pós-deploy** (leitura; o texto executado é o `prosrc` implantado,
provado no próprio servidor):
- 72066, 72078, 72080 e 72097 aprovados;
- 71752 recusado por `AUSENCIA_EXPLICADA` e `DIFERENCA_DENTRO_DA_MARGEM_SEGURA`.

Nenhum acordo foi registrado.

## COMO RECONFERIR

```sql
select version, name, array_length(statements, 1) as n,
       md5(array_to_string(statements, E'\n')) as md5_prod
  from supabase_migrations.schema_migrations
 where version = '20260917003123';
```

```bash
perl -0pe 's/\n\z//' supabase/ledger/2026-09/20260917003123__recuperacao_acordo_pago_sem_importacao.sql | md5 -q
```

Os dois devolvem `655f169b6a1e4178ecca1353ab9c367a`.
