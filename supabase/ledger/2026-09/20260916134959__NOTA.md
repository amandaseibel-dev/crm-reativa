# 20260916134959 — reprocessamento controlado dos 69 pagamentos do backfill #392

| | |
|---|---|
| **Versão** | `20260916134959` |
| **Nome em produção** | `reprocessa_69_pagamentos_do_backfill_392` |
| **SQL exato** | [`20260916134959__reprocessa_69_pagamentos_do_backfill_392.sql`](20260916134959__reprocessa_69_pagamentos_do_backfill_392.sql) |
| **md5 (statement de produção)** | `9551b2a77c171390a41a16e607b71aad` |
| **SHA256 (statement de produção)** | `ce529063652bfa074ee852519e2b3293e6aca9c05cbb7c32775c0c3ef9b06b19` |
| **Bytes** | **31.267 bytes** em UTF-8 |
| **Caracteres** | **31.263** (os 4 bytes de diferença são os 4 `·` da mensagem final) |
| **Statements** | 1 |
| **Aplicada em** | 2026-09-16, 13:49:59 UTC |
| **Classificação** | `CORRECAO_DADO` |
| **Par na trilha executável** | `supabase/migrations/20260916150000_reprocessa_69_pagamentos_do_backfill_392.sql` (PR #394) |

## PARIDADE DE CONTEÚDO — MD5 BRUTOS DIFERENTES, CONTEÚDO SQL IDÊNTICO

Os md5 brutos **não são iguais**, e isso é registrado como é:

| | Bytes (UTF-8) | Caracteres | Linhas | md5 | SHA256 |
|---|---:|---:|---:|---|---|
| `supabase/migrations/20260916150000_…` (bruto) | 31.268 | 31.264 | 528 + `\n` final | `fe95cdb5bcda4bf0f1d784ef847132fb` | `03a2c975…1985fb` |
| `supabase/migrations/20260916150000_…` sem o `\n` final | 31.267 | 31.263 | 528 | `9551b2a77c171390a41a16e607b71aad` | `ce529063…b06b19` |
| Statement aplicado em produção | 31.267 | 31.263 | 528 | `9551b2a77c171390a41a16e607b71aad` | `ce529063…b06b19` |

Comparação feita em 16/09/2026, somente leitura, com prova independente de cada lado:

* **primeiro byte divergente:** byte **31.268** do arquivo executável — `0x0a`, o newline
  que encerra a linha 528 (`$reprocessa$;`). O statement termina no byte 31.267;
* **total de bytes divergentes: 1.** Nenhum outro;
* os 31.267 primeiros bytes do arquivo são iguais ao statement: md5 e SHA256 do
  conteúdo inteiro batem, a assinatura linha a linha das 528 linhas
  (`md5` de `n:md5(linha)`) bate (`8325413dd5389892c096332398e56786` nos dois
  lados), os 20 primeiros e os 14 últimos caracteres batem em hexadecimal, e os
  4 caracteres não-ASCII (`·`) estão nos dois;
* sem `\r` e sem BOM nos dois lados.

**A diferença é só o newline final do arquivo. Não há diferença de SQL.** Aplicada
a normalização documentada no `LEIA-ME.md` (`perl -0pe 's/\n\z//'`), o arquivo
executável devolve o md5 de produção.

O arquivo deste diretório é o statement mais o newline POSIX, pela convenção do
`LEIA-ME.md`: por isso ele é, byte a byte, igual ao arquivo executável
(`fe95cdb5…` bruto) e devolve `9551b2a7…` na reconferência.

A versão também difere (`20260916150000` no repositório, `20260916134959` em
produção), pela causa já registrada em `20260913013957__NOTA.md`: o
`apply_migration` gera a `version` pelo relógio UTC do instante.

## O QUE RODOU

Um bloco `DO`, em uma transação, que **não tem UPDATE, INSERT ou DELETE próprio**:

1. `lock_timeout` de 3 s;
2. confere o motor (`pagamento_conciliar_um`, md5 `fa3d64ad…`), os gatilhos
   `trg_recalc_parcela` e `trg_acordo_fecha_com_a_ultima_parcela` (presentes,
   `O`, definição e corpo aprovados) e `baixa_pelo_relatorio`,
   `baixa_por_documento` e `consulta_portador` desligadas;
3. reconstrói o lote das 748 parcelas do #392 pelos ids da trilha e exige o
   SHA256 canônico `53d4b1a0…`;
4. monta o conjunto — `AGUARDANDO_AMARRACAO` ligado ao lote, sem decisão na fila,
   na ordem da fila do motor — e exige 69 pagamentos, SHA256 `bddd4d68…`,
   R$ 40.831,07, 69 parcelas, 23 acordos, 23 alunos, 21 quitações previstas,
   0 `AGUARDANDO_AMARRACAO` fora do conjunto, 95 pagamentos do lote sem status,
   restante da fila `AGUARDANDO_ACORDO=38;PARCELA_JA_PAGA=6` e a fila dos 69
   com 69 linhas sem decisão;
5. trava as linhas pertinentes e captura os PRE;
6. roda a **prévia do próprio motor**, `pagamento_conciliar_um(id, false)`, para
   os 69 — qualquer resultado diferente de `BAIXADO` na parcela prevista abortaria
   antes de qualquer baixa;
7. chama o motor, `pagamento_conciliar_um(id, true)`, uma vez para cada um, na
   ordem da fila, exigindo `BAIXADO` e `baixou`;
8. confere os POS e aborta se qualquer um divergir.

Nenhuma migration de prévia separada foi registrada: depois de `20260916112956`,
a única versão em `schema_migrations` é esta.

## RESULTADO MEDIDO

Leitura às 13:58 UTC, sem escrita:

| Conferência | Resultado |
|---|---|
| pagamentos do lote | **`BAIXADO` = 69** · sem `status_conciliacao` = **95** |
| SHA256 dos 69 baixados (mesma fórmula da migration) | `bddd4d68c2da14e24f9f0c95c56629c1ca275bad1b91b1cf1ffaa6cd96c332eb` — o aprovado |
| valor | **R$ 40.831,07** |
| parcelas `PAGO` com `origem_baixa_ref` do próprio pagamento | **69**, origem `GATILHO_IMPORTACAO`, às 13:49:59 UTC |
| acordos do conjunto | **23**: **21 `QUITADO`**, 2 `ATIVO` |
| alunos | 23 |
| fila dos 69 | 69 linhas, todas `RESOLVIDO_AUTOMATICO` · `BAIXADO` · `conciliacao@sistema` |
| restante da fila do motor | `AGUARDANDO_ACORDO=38;PARCELA_JA_PAGA=6` — inalterado |
| `fluxo_pagamentos_config` | `baixa_pelo_relatorio`, `baixa_por_documento`, `consulta_portador`, `vinculo_por_negociacao` = `false`; `amarrar_boleto`, `pos_importacao`, `sinalizar_duplicado` = `true` |
| gatilhos | `trg_recalc_parcela` e `trg_acordo_fecha_com_a_ultima_parcela` em `O`, md5 `8632b2fc…` e `9a230134…` |
| registros com esse `name` no ledger | 1 |

`baixa_pelo_relatorio` **permaneceu pausada** (`20260916112756`).

## CLASSIFICAÇÃO

`CORRECAO_DADO`: o bloco não cria nem altera estrutura, não desliga gatilho e não
muda configuração — conferido no próprio bloco e na leitura acima. O que mudou
foram dados, todos pelo motor existente: `pagamentos`, `parcelas`,
`fila_pagamento_sem_vinculo`, `acordos` (pelo gatilho da última parcela) e
`alunos`/`casos` (pelo recálculo). Mesmo tratamento do backfill `20260916112956`.
Como naquele caso, o par em `supabase/migrations/` existe porque a gestão exigiu
PR, testes e revisão antes da aplicação (PR #394).

## FORA DO ESCOPO

Os 95 pagamentos do lote sem `status_conciliacao`, religar `baixa_pelo_relatorio`
e novas importações dependem de autorização expressa.

## COMO RECONFERIR

```sql
select version, name, array_length(statements, 1) as n,
       md5(statements[1]) as md5_prod,
       octet_length(statements[1]) as bytes, length(statements[1]) as chars
  from supabase_migrations.schema_migrations
 where version = '20260916134959';
```

```bash
perl -0pe 's/\n\z//' supabase/ledger/2026-09/20260916134959__*.sql | md5 -q
perl -0pe 's/\n\z//' supabase/migrations/20260916150000_reprocessa_69_pagamentos_do_backfill_392.sql | md5 -q
md5 -q supabase/migrations/20260916150000_reprocessa_69_pagamentos_do_backfill_392.sql
```

Os três primeiros devolvem `9551b2a77c171390a41a16e607b71aad`; o último,
`fe95cdb5bcda4bf0f1d784ef847132fb`.
