# 20260917100621 — pagamentos_trava usa o resultado da prévia (PR #401)

| | |
|---|---|
| **Versão** | `20260917100621` |
| **Nome em produção** | `pagamentos_trava_usa_resultado_da_previa` |
| **SQL exato** | [`20260917100621__pagamentos_trava_usa_resultado_da_previa.sql`](20260917100621__pagamentos_trava_usa_resultado_da_previa.sql) |
| **md5 (statement de produção)** | `c6aff4a41945bf215b4b2392d32ed979` |
| **SHA256 (statement de produção)** | `442febf1496053fa0fd2cb1aede196d0ee10d41700260c6749bb95ae0e8fc52e` |
| **Bytes** | **7,736 bytes** em UTF-8 |
| **Caracteres** | **7,736** |
| **Statements** | 1 |
| **Aplicada em** | 2026-09-17 10:06:21 UTC |
| **Classificação** | `ESTRUTURAL` |
| **Par na trilha executável** | [`supabase/migrations/20260917100000_pagamentos_trava_usa_resultado_da_previa.sql`](../../migrations/20260917100000_pagamentos_trava_usa_resultado_da_previa.sql) — mesmo SQL, version diferente |

A version difere da do arquivo em `supabase/migrations/` porque o
`apply_migration` grava a version pelo relógio UTC do instante. A diferença é
preservada de propósito. O statement de produção não tem o newline final; o
arquivo deste diretório tem, pela convenção POSIX do `LEIA-ME.md`.

## CONTEXTO

Implantação do PR #401, autorizada pela gestão em 17/09/2026, **sem registro de
acordo real**. A migration entrou antes do front: aplicada às 10:06:21 UTC, com
o merge (`6323718`) às 10:08:32 UTC.

A auditoria da fila tinha achado 27 linhas com "Registrar acordo à vista", das
quais só 12 passavam na prévia do registro. `pagamentos_trava` já executava
`acordo_avista_previa(id, null)`, mas ignorava `aprovado` e `bloqueios`.

**Preflight** (leitura, 10:05 UTC):
- head do PR = `7793e03`, com CI remoto verde;
- `main` só avançou com o ledger do PR #400, sem interseção com o PR;
- `md5(prosrc)` de `pagamentos_trava` = `482299bf8488c74018f201279614302c`, o
  corpo que serviu de base para a migration;
- permissões e portão:
  - 1 sobrecarga;
  - ACL `{postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}`;
  - sem anon/PUBLIC;
  - portão da gestão antes da prévia;
  - sem escrita;
- o front então publicado, testado com as seis travas novas, não oferecia ação
  nenhuma (12/12).

## O QUE FEZ

Trocou só o corpo de `public.pagamentos_trava(uuid[])`. A assinatura, o retorno e
as permissões ficaram iguais, e nenhum dado foi escrito. Quando a linha seria
`ACORDO_AVISTA_AUSENTE` e a prévia não aprova, a trava passa a ser o motivo da
própria prévia:
- `ACORDO_AVISTA_ALUNO_ENCERRADO`;
- `ACORDO_AVISTA_OPERADOR_NAO_CADASTRADO`;
- `ACORDO_AVISTA_OUTRO_BLOQUEIO`;
- `ACORDO_AVISTA_SEM_MENSALIDADE_ELEGIVEL`;
- `ACORDO_AVISTA_FORA_DA_MARGEM`;
- `ACORDO_AVISTA_SEM_COMBINACAO_SEGURA`.

A função não faz simulação nova nem busca combinação menor de mensalidades.

A explicação completa da regra está no cabeçalho do próprio SQL.

**Depois** (leitura, 10:06 UTC):
- a função:
  - `md5(prosrc)` = `33e19e85eebbf989673df21f4b592b94`;
  - 1 sobrecarga, ACL idêntica, sem anon/PUBLIC;
  - sem escrita, uma chamada à prévia, portão antes da prévia;
- ficaram iguais ao preflight:
  - o md5 do motor, dos vínculos, da importação, da consulta ao portador, da
    prévia e do registro;
  - os 56 gatilhos (md5 `b2604267`) e os 29 jobs do cron (md5 `1310fec7`);
  - `fluxo_pagamentos_config` (md5 `674351f9`), com `consulta_portador`
    desligada.

**Auditoria antes do front** (leitura; prévia implantada provada por md5):
- 12 registráveis;
- 10 fora da margem;
- 1 aluno encerrado;
- 1 operador não cadastrado;
- 2 sem mensalidade elegível;
- 1 sem combinação segura.

Nenhuma ação que a prévia recusa, nenhuma aprovada sem ação, e as outras 16
linhas da fila não mudaram.

**Tela em produção** (sessão da gestão, 10:10 UTC):
- só as 12 aprovadas oferecem a ação, e as 15 mostram o motivo, sem botão;
- 71752 continua sem ação;
- 72066, 72078, 72080 e 72097 foram abertos só em simulação (`p_confirmar`
  false);
- o CPF completo não aparece no DOM;
- `pagamentos_trava` levou 2.125 ms no navegador.

**Prova final** (10:13 UTC, contra o preflight):
- 4.028 acordos, 15.180 parcelas, 4.557 PAGO e 4.658 vínculos;
- nenhuma baixa, acordo ou parcela nova;
- 0 auditorias `RECUPERACAO_ACORDO_PAGO_SEM_IMPORTACAO`;
- os 43 pagamentos da fila, idênticos.

Nenhum acordo foi registrado.

## COMO RECONFERIR

```sql
select version, name, array_length(statements, 1) as n,
       md5(array_to_string(statements, E'\n')) as md5_prod
  from supabase_migrations.schema_migrations
 where version = '20260917100621';
```

```bash
perl -0pe 's/\n\z//' supabase/ledger/2026-09/20260917100621__pagamentos_trava_usa_resultado_da_previa.sql | md5 -q
```

Os dois devolvem `c6aff4a41945bf215b4b2392d32ed979`.
