# 20260914090235 — ajuste de valor cobrável por título

| | |
|---|---|
| **Versão** | `20260914090235` |
| **Nome em produção** | `ajuste_de_valor_cobravel` |
| **SQL exato** | [`20260914090235__ajuste_de_valor_cobravel.sql`](20260914090235__ajuste_de_valor_cobravel.sql) |
| **md5** | `42b5754d2ca69024e36fd273b8abb7ec` |
| **Bytes** | **34.348 bytes** em UTF-8 |
| **Caracteres** | **34.325** (`length()` no PostgreSQL) |
| **Aplicada em** | 2026-09-14, 09:02:35 UTC |
| **Classificação** | `ESTRUTURAL` |
| **Par na trilha executável** | `supabase/migrations/20260914100000_ajuste_de_valor_cobravel.sql` (PR #371) |

## PARIDADE DE CONTEÚDO — BATE

| | Bytes (UTF-8) | Caracteres (`length()`) | md5 |
|---|---:|---:|---|
| `supabase/migrations/20260914100000_…` (sem o `\n` final) | 34.348 | 34.325 | `42b5754d2ca69024e36fd273b8abb7ec` |
| Statement aplicado em produção | 34.348 | 34.325 | `42b5754d2ca69024e36fd273b8abb7ec` |

A única divergência entre as trilhas é a **versão**, pela causa já registrada em
`20260913013957__NOTA.md`: o `apply_migration` gera a `version` pelo relógio UTC
do instante e ignora a pretendida (`20260914100000`). Preservada de propósito.

Nota sobre a contagem, explícita porque os dois números são diferentes aqui:
o SQL tem **34.348 bytes** em UTF-8 e **34.325 caracteres**. A diferença de 23
vem dos acentos herdados de `recalcular_situacao_aluno` — `length()` no
PostgreSQL conta caracteres, `wc -c` conta bytes. A coluna `bytes` do
`INDICE.tsv` registra os **34.348 bytes**. O md5 é a prova de igualdade, e
ele bate nos dois lados.

## O QUE RODOU

Sete blocos, nesta ordem:

1. **Colunas** — `valor_cobranca_ajustado`, `motivo_ajuste_valor`,
   `valor_ajustado_por`, `valor_ajustado_em` em `acordos_titulos`, mais um
   `CHECK ... > 0` criado `not valid` (sem varrer a tabela).
2. **Portão** — `crm_usuario_pode_ajustar_valor()`, com os dois e-mails
   cadastrados: `amanda.seibel@` (gerência) e `cobranca04@` (Fernanda,
   supervisor). `cobranca07@` é a Amanda Borges, do administrativo — outra
   pessoa, fora desta permissão de propósito.
3. **Histórico** — tabela `titulo_valor_ajuste_historico`, RLS ligada, leitura
   só para quem pode ajustar, escrita só pelo RPC. Sem `ON DELETE CASCADE`:
   apagar um título não apaga o evento.
4. **Elegibilidade** — `titulo_ajuste_valor_bloqueio(uuid)`.
5. **Guarda de tabela** — `tg_titulo_ajuste_valor_protegido()` +
   `trg_titulo_ajuste_valor_protegido`, `BEFORE INSERT OR UPDATE OF` as 4 colunas.
6. **RPC única** — `titulo_ajustar_valor_cobravel(uuid, numeric, text)`.
7. **Leitores** — `aluno_saldo_pendente_detalhe`, `recalcular_situacao_aluno` e
   `resumo_carteira_operador`, cada um com UMA troca cirúrgica sobre a foto de
   produção guardada em `supabase/audits/*_producao_20260914.sql`.

Nenhum backfill. Nenhum título existente alterado.

## MEDIÇÃO ANTES × DEPOIS

Medida em duas rodadas de leitura, antes e depois da aplicação.

| Item | Antes | Depois |
|---|---|---|
| As 4 colunas existem | não | **sim** (`numeric`, `text`, `text`, `timestamptz`) |
| Tabela de histórico existe | não | **sim** — 0 linhas |
| Títulos com `valor_cobranca_ajustado` | — | **0** |
| `acordos_titulos` total | 47.945 | 47.945 |
| `n_tup_ins / upd / del` de `acordos_titulos` | 12.683 / 44.727 / 10.281 | **iguais** |
| `alunos.saldo_total` | R$ 45.204.104,20 | **igual** |
| `casos.saldo_total` | R$ 46.630.865,67 | **igual** |
| `pagamentos` `ins/upd/del` | 2.435 / 15.713 / 7 | **iguais** |
| `parcelas` | 9.578 / 64.956 / 2.380 | **iguais** |
| `acordos` | 1.268 / 11.732 / 590 | **iguais** |
| `baixas_pagamento` | 2.561 / 1.606 / 1 | **iguais** |
| Crons ativos | 27 de 29 | **27 de 29** |
| Versões em `schema_migrations` | 1.242 | 1.243 (só esta) |

Estados de `acordos_titulos`, idênticos nos dois lados: `ABERTO/em_aberto`
39.404 · `PAGO/quitada` 5.083 · `NEGOCIADO/vinculada` 3.055 ·
`CANCELADA/cancelada` 262 · `DUPLICADA/em_aberto` 141.

## OS TRÊS LEITORES MANTIVERAM TUDO

| Função | Owner | `SECURITY DEFINER` | ACL |
|---|---|---|---|
| `aluno_saldo_pendente_detalhe` | `postgres` | `true` | `{postgres, authenticated, service_role}` |
| `recalcular_situacao_aluno` | `postgres` | `true` | idem |
| `resumo_carteira_operador` | `postgres` | **`false`** | idem |

Os três valores são os mesmos de antes da aplicação — inclusive o
`definer=false` do `resumo_carteira_operador`, que é `SECURITY INVOKER` e
continuou sendo.

## PERMISSÃO DOS OBJETOS NOVOS

| Função | `PUBLIC` | `anon` | `authenticated` | `service_role` |
|---|---|---|---|---|
| `crm_usuario_pode_ajustar_valor` | não | não | sim | sim |
| `titulo_ajuste_valor_bloqueio` | não | não | sim | sim |
| `titulo_ajustar_valor_cobravel` | não | não | sim | sim |
| `tg_titulo_ajuste_valor_protegido` | **sim** | **sim** | sim | sim |

**Achado registrado:** a função de gatilho ficou com EXECUTE para `PUBLIC` — eu
revoguei os três RPCs e esqueci dela. **Não é explorável**: confirmado em
produção que a chamada direta devolve
`0A000 trigger functions can only be called as triggers`. Mesmo assim está fora
do padrão fechado no PR #368, e a correção é uma linha — vai em migration
própria, ainda não aplicada.

## O QUE COMPILOU DE VERDADE

`check_function_bodies` está `on` em produção, e o `CREATE` passou: os corpos
plpgsql das quatro funções novas e dos três leitores **foram validados** no
momento da aplicação. Criados: 4 funções + 1 tabela + 1 constraint + 1 gatilho.

A RPC **não foi chamada** com título real. Nenhum ajuste foi feito.
